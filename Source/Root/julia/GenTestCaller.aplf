GenTestCaller←{
     ⍝ ⍺ is module name; ⍵ is decorated unittest()
     z←'module TestModule',NL,NL
     z←z,' include("',⍺,'.jl")',NL
     z←z,' z  = ',⍺,'.',⍵,'()',NL
     z←z,' return z',NL,'end',NL
     z
 }
