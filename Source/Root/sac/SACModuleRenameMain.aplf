 SACModuleRenameMain←{
     ⍝ Rename main() to allow us to gen a SaC module
     ⍝ E.g., main -> iotan_main
     ⍝   Our intent is to avoid name collisions on main(), and besides,
     ⍝   SaC does not like main() in a module.
     ⍝  If there is no main(), we merely generate the module
     ÷0 ast←(~isDfnMain¨⍵) : ast
     ast←D ssaast⌷⍵
     smn←⍺ SACModuleName ast
     ast[dfnname;asttarget]←E smn
     z←⍵
     z[ssaast]←E ast
     (E z), E smn
 }
