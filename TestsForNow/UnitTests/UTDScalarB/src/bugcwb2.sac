use ArrayArith:{&,*,<=,<,shape};
use Array:{sel};

// From ArrayArith
inline bool all( bool[*] a)
{
  res = with {
          (0*shape(a) <= iv < shape(a)) : a[iv];
        } : fold( &, true );
  
  return( res);
}
int main()
{ 
BS0_0= false;
 A_80 = _neq_SxS_(BS0_0, BS0_0);
 r_10 = _toi_S_(all(A_80));
 return(r_10);
}

